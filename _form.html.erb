<%= form_with(model: student) do |form| %>
<% if student.errors.any? %>
<div id="error_explanation" class="bg-red-50 p-6 rounded text-red-800">
  <h2 class="text-lg font-semibold mb-3"><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

  <ul class="list-disc leading-relaxed">
    <% student.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <div class="mb-6">
        <%= form.label :name, class: label_class %>
          <%= form.select :name, options_from_collection_for_select(Student.all, 'name', selected = book.name), class: label_class %>
      </div>
  <div class="mb-6">
        <%= form.label :string, class: label_class %>
          <%= form.text_field :string, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :description, class: label_class %>
          <%= form.text_field :description, class: input_class %>
      </div>

<%= form.submit class: button_class(theme: 'primary') %>

<% if student.persisted? %>
<%= link_to "Cancel", student, class: "text-neutral-700 underline inline-block ml-3" %>
<% else %>
<%= link_to "Cancel", students_path, class:  "text-neutral-700 underline inline-block ml-3" %>
<% end %>
<% end %>
